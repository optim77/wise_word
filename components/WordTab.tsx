import { View } from "react-native";

type WordTabProps = {
    word: string
    meaning: string;
    likes: number;
    category: string;
}

export const WordTab = ({word, meaning, likes, category}: WordTabProps) => {

    return (
        <View>
        </View>
    )
}